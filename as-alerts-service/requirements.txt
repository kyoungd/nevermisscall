# AS Alerts Service Requirements
# Dependencies specific to this service (shared library provides most common dependencies)

# Web Framework
fastapi>=0.100.0

# ASGI Server
uvicorn[standard]>=0.20.0

# Development and Testing (for unit tests only)
pytest>=7.0.0
pytest-asyncio>=0.21.0
httpx>=0.25.0  # For testing FastAPI apps

# Note: The following dependencies are provided by the shared library:
# - asyncpg (database)
# - pydantic (data validation) 
# - email-validator
# - python-dateutil
# - PyJWT (authentication)
# - All database, config, logging, and utility functions